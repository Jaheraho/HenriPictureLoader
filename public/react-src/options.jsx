export default class Options extends React.Component{
    render(){
        return (
            <div>1Penis Welt! {this.props.blub}</div>
        )
    }
}

Options.propTypes = {
}

Options.defaultProps = {
}
